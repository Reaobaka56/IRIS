# Reference snippet for `grammars.yml`.
#
# The final vendor path is determined by the repository name passed to
# `script/add-grammar`. This example assumes a dedicated permissively licensed
# grammar repository named `iris-tmLanguage`.

vendor/grammars/iris-tmLanguage:
  - source.iris
