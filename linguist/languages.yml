# Add this entry alphabetically under "I" in lib/linguist/languages.yml.
# Leave `language_id` out here; Linguist generates it with `script/update-ids`.

IRIS:
  type: programming
  color: "#6A0DAD"
  aliases:
    - iris-lang
  extensions:
    - ".iris"
  tm_scope: source.iris
  ace_mode: text
  interpreters:
    - iris
