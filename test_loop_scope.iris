def test() -> i64 {
    val x : list<f64> = list()
    list_push(x, 3.14);
    for i in 0..5 {
        val inner : list<i64> = list()
        list_push(inner, i)
    }
    0
}

def main() -> i64 {
    test()
}
